import { DecreaseTextPipe } from './decrease-text.pipe';

describe('DecreaseTextPipe', () => {
  it('create an instance', () => {
    const pipe = new DecreaseTextPipe();
    expect(pipe).toBeTruthy();
  });
});
